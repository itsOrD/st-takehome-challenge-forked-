const Article = () => {
  return (
    <div>
      hi, this is a test to see if I have to use codesandbox or if i can pull
      local and then push back to sandbox for submission
    </div>
  );
};

export default Article;
